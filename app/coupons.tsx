import { useFocusEffect } from 'expo-router'; import { useCallback, useState } from 'react'; import { ActivityIndicator, SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native'; import { Coupon, getAvailableCoupons } from '@/services/api/coupons';
export default function CouponsScreen(){const [items,setItems]=useState<Coupon[]>([]);const [loading,setLoading]=useState(true);const load=useCallback(async()=>{try{setItems(await getAvailableCoupons())}finally{setLoading(false)}},[]);useFocusEffect(useCallback(()=>{void load()},[load]));if(loading)return <SafeAreaView style={s.center}><ActivityIndicator color="#00A651"/></SafeAreaView>;return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><Text style={s.title}>Offers & coupons</Text><Text style={s.sub}>Apply a code during booking to get the discount.</Text>{items.map(c=><View key={c.id} style={s.card}><Text style={s.code}>{c.code}</Text><Text style={s.value}>{c.discountType==='PERCENTAGE'?`${c.discountValue}% off`:`INR ${c.discountValue} off`}</Text><Text style={s.detail}>{c.description||'Valid on eligible services'}</Text></View>)}{!items.length?<Text style={s.sub}>No active coupons right now.</Text>:null}</ScrollView></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#F8FAFC'},center:{flex:1,alignItems:'center',justifyContent:'center'},content:{padding:16,gap:12},title:{fontSize:24,fontWeight:'900',color:'#0F172A'},sub:{fontSize:13,color:'#64748B'},card:{backgroundColor:'#FFF',borderWidth:1,borderColor:'#BBF7D0',borderRadius:16,padding:16},code:{fontSize:18,fontWeight:'900',color:'#00A651'},value:{fontSize:16,fontWeight:'800',color:'#0F172A',marginTop:6},detail:{fontSize:13,color:'#64748B',marginTop:5}});
