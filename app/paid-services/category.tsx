import { useLocalSearchParams, useRouter , useFocusEffect } from 'expo-router';
import { useCallback, useState } from 'react';
import { ActivityIndicator, Pressable, SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { getPaidServices, PaidService } from '@/services/api/paid-services';
export default function PaidServiceCategory() { const { id, name } = useLocalSearchParams<{id:string;name:string}>(); const router = useRouter(); const [items,setItems]=useState<PaidService[]>([]); const [loading,setLoading]=useState(true); useFocusEffect(useCallback(()=>{ void getPaidServices(`categoryId=${encodeURIComponent(id || '')}&limit=100`).then(r=>setItems(r.items)).finally(()=>setLoading(false)); },[id])); return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Pressable onPress={()=>router.back()}><Text style={styles.back}>‹ Back</Text></Pressable><Text style={styles.title}>{name}</Text>{loading?<ActivityIndicator color="#00A651" style={{marginTop:30}}/>:items.map(item=><Pressable key={item.id} style={styles.card} onPress={()=>router.push(`/paid-services/${item.slug}`)}><Text style={styles.name}>{item.name}</Text><Text style={styles.desc}>{item.description || 'Choose a package to see price and inclusions.'}</Text><Text style={styles.link}>Explore packages →</Text></Pressable>)}</ScrollView></SafeAreaView>; }
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:'#F8FAFC'},content:{padding:16},back:{color:'#00A651',fontWeight:'800',fontSize:15},title:{fontSize:24,fontWeight:'900',color:'#0F172A',marginVertical:16},card:{backgroundColor:'#FFF',padding:16,borderRadius:14,borderWidth:1,borderColor:'#E2E8F0',marginBottom:10},name:{fontSize:16,fontWeight:'900',color:'#0F172A'},desc:{fontSize:13,color:'#64748B',marginTop:7,lineHeight:18},link:{fontSize:13,color:'#00A651',fontWeight:'800',marginTop:12}});
